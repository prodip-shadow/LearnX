import AddResourceForm from '@/components/ResourceComponents/AddResourceForm';

export const metadata = {
  title: 'Add Resource - Teacher Dashboard',
};

export default function AddResourcePage() {
  return <AddResourceForm />;
}
